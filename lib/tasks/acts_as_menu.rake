namespace :mouse_s do
  desc 'create model order'
  task :order do 
    #puts "Creating #{::Rails.root.to_s }/config/mouse.yml"
    #mouse = File.new("#{::Rails.root.to_s}/config/mouse.yml", "w") 
    #mouse.puts("hello")
    #puts "Creating #{ ::Rails.root.to_s }/model/mouse.rb"
    #template "mouse.rb", "model/devise.rb"
  end
end
